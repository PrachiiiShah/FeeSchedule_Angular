import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
import { AuthService } from '../Registration/auth.service';
@Injectable({
  providedIn: 'root'
})
export class HistoryService {
// histoeySubject=new  BehaviorSubject([]);
// history=[];
  constructor( ) { }
}
